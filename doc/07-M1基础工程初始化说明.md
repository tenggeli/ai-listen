# listen M1 基础工程初始化说明

## 1. 本次初始化内容
- 新增 `backend/` Go 服务骨架
- 新增 `web/apps/user-web/` 用户端基础 Vite 工程
- 新增 `web/apps/admin-web/` 管理后台基础 Vite 工程
- 新增 `app/listen-app/` App 目录占位
- 新增 `deploy/nginx/` 和 `deploy/docker/` 示例部署配置
- 新增根目录 `README.md` 与 `.gitignore`

## 2. 当前可用能力
- 后端已具备服务启动入口和健康检查接口：
  - `/`
  - `/api/v1/health`
- 用户端和后台端已具备基础入口页，可继续扩展为正式页面

## 3. 建议下一步
1. 生成数据库设计文档
2. 初始化用户、服务提供方、订单三大核心模块
3. 把 `doc/*.html` 设计稿落成 Vue 页面
4. 增加环境变量和数据库连接配置

## 4. 注意事项
- 本次为工程初始化，尚未执行 `go mod tidy`、`npm install` 和构建命令。
- 如果下一步要验证运行，需要本地安装 Go 和 Node.js，并补装依赖。

